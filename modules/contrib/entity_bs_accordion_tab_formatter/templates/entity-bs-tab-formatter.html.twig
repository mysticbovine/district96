{#
/**
 * @file
 * Default theme implementation for a feed icon.
 *
 * Available variables:
 * - url: An internal system path or a fully qualified external URL of the feed.
 * - icon: The rendered HTML (<img> tag) for the feed icon image.
 * - attributes: Remaining HTML attributes for the feed link.
 *   - title: A descriptive title of the feed link.
 *   - class: HTML classes to be applied to the feed link.
 *
 * @see template_preprocess_feed_icon()
 *
 * @ingroup themeable
 */
#}
<div class="entity-bs-tab-formatter"{{ attributes }}>
  <ul class="nav nav-tabs" role="tablist">
    {% for fieldkey, fieldname in tabs %}
      <li{{ create_attribute(fieldname.li_attributes) }}><a{{ create_attribute(fieldname.header_attributes) }}> {{ fieldname.title }} </a></li>
    {% endfor %}
  </ul>
  <div class="tab-content">
    {% for fieldkey, fieldname in tabs %}
    <div{{ create_attribute(fieldname.body_attributes) }}>
      {{ fieldname.body }}
    </div>
    {% endfor %}
  </div>
</div>
